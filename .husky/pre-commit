pnpm exec lint-staged
pnpm turbo type-check --filter='[HEAD^1]'
if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact
else
  echo "warning: gitleaks not found, skipping secret scan (install with: brew install gitleaks)"
fi
